# Claude / agent entrypoint

This repo's agent configuration lives in **`AGENTS.md`**. Treat that file as the source of truth.

- Read and follow **`AGENTS.md`** for all agent skills, issue tracker, triage labels, and domain-doc conventions.
- Detailed docs: `docs/agents/issue-tracker.md`, `docs/agents/triage-labels.md`, `docs/agents/domain.md`.

Do not duplicate skill config here — edit `AGENTS.md` and `docs/agents/*.md` instead.
